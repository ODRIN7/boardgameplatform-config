eureka:
  instance:
    prefer-ip-address: true
  client:
    serviceUrl:
      defaultZone: http://registry:8081/eureka/

security:
  oauth2:
    resource:
      user-info-uri: http://auth-service:8083/uaa/users/current
spring:
  rabbitmq:
    host: rabbitmq

